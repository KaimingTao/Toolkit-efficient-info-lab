[project]
name = "csv-file"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
]
