[project]
name = "efficient-info-lab"
version = "0.1.0"
description = "CSV utilities and workflow examples for efficient information handling"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
]
